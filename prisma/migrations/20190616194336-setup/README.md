# Migration `20190616194336-setup`

This migration has been generated by timsuchanek at 6/16/2019, 7:43:36 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."Blog" ("id" INTEGER DEFAULT 0 NOT NULL, "name" TEXT DEFAULT '' NOT NULL, "viewCount" INTEGER DEFAULT 0 NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Author" ("id" INTEGER DEFAULT 0 NOT NULL, "name" TEXT, "blog" INTEGER REFERENCES Blog(id) NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Post" ("id" INTEGER DEFAULT 0 NOT NULL, "title" TEXT DEFAULT '' NOT NULL, "blog" INTEGER REFERENCES Blog(id) NOT NULL, "author" INTEGER REFERENCES Author(id), PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Post_tags" ("nodeId" INTEGER REFERENCES Post(id) NOT NULL, "position" INTEGER NOT NULL, "value" TEXT NOT NULL, PRIMARY KEY ("nodeId","position"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190616194336-setup
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photon-js"
+  output   = "./generated/photon"
+}
+
+model Blog {
+  id        Int      @id
+  name      String
+  viewCount Int
+  posts     Post[]
+  authors   Author[]
+}
+
+model Author {
+  id    Int     @id
+  name  String?
+  posts Post[]
+  blog  Blog
+}
+
+model Post {
+  id    Int      @id
+  title String
+  tags  String[]
+  blog  Blog
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190616194336-setup)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190616194336-setup'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
