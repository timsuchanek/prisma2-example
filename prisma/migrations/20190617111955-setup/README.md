# Migration `20190617111955-setup`

This migration has been generated by timsuchanek at 6/17/2019, 11:19:55 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Blog" ("id" INTEGER DEFAULT 0 NOT NULL, "name" TEXT DEFAULT '' NOT NULL, "viewCount" INTEGER DEFAULT 0 NOT NULL, PRIMARY KEY ("id"));

INSERT INTO "new_Blog" ("id","name","viewCount") SELECT "id","name","viewCount" from "Blog"

DROP TABLE "lift"."Blog";

ALTER TABLE "lift"."new_Blog" RENAME TO "Blog";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190616194336-setup..20190617111955-setup
--- datamodel.dml
+++ datamodel.dml
@@ -5,9 +5,8 @@
 }
 generator photon {
   provider = "photon-js"
-  output   = "./generated/photon"
 }
 model Blog {
   id        Int      @id
```

## Photon Usage

You can use a specific Photon built for this migration (20190617111955-setup)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190617111955-setup'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
