# Migration `20190617215348`

This migration has been generated by timsuchanek at 6/17/2019, 9:53:48 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Blog"("id" INTEGER NOT NULL DEFAULT 0 ,"name" TEXT NOT NULL DEFAULT '' ,"viewCount2" INTEGER NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"));

INSERT INTO "new_Blog" ("id","name") SELECT "id","name" from "Blog"

DROP TABLE "lift"."Blog";

ALTER TABLE "lift"."new_Blog" RENAME TO "Blog";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190617111955-setup..20190617215348
--- datamodel.dml
+++ datamodel.dml
@@ -4,15 +4,23 @@
   default  = true
 }
 generator photon {
-  provider = "photon-js"
+  provider = "photonjs"
 }
+generator nexusThingy {
+  provider = "nexus-prisma"
+}
+
+generator otherThingy {
+  provider = "prisma-test-utils"
+}
+
 model Blog {
   id        Int      @id
   name      String
-  viewCount Int
+  viewCount2 Int
   posts     Post[]
   authors   Author[]
 }
@@ -27,5 +35,5 @@
   id    Int      @id
   title String
   tags  String[]
   blog  Blog
-}
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190617215348)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190617215348'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
