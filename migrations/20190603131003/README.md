# Migration `20190603131003`

This migration has been generated by timsuchanek at 6/3/2019, 1:10:03 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "migration_engine"."Blog" ("id" INTEGER NOT NULL, "name" TEXT NOT NULL, "viewCount" INTEGER NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "migration_engine"."Author" ("id" INTEGER NOT NULL, "name" TEXT, PRIMARY KEY ("id"));

CREATE TABLE "migration_engine"."Post" ("id" INTEGER NOT NULL, "title" TEXT NOT NULL, "blog" INTEGER REFERENCES Blog(id) NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "migration_engine"."Post_tags" ("nodeId" INTEGER REFERENCES Post(id) NOT NULL, "position" INTEGER NOT NULL, "value" TEXT NOT NULL, PRIMARY KEY ("nodeId","position"));

CREATE TABLE "migration_engine"."_AuthorToBlog" ("A" INTEGER REFERENCES Author(id) NOT NULL, "B" INTEGER REFERENCES Blog(id) NOT NULL);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration last migration id..20190603131003
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,20 @@
+model Blog {
+  id: Int @id
+  name: String
+  viewCount: Int
+  posts: Post[]
+  authors: Author[]
+}
+
+model Author {
+  id: Int @id
+  name: String?
+  authors: Blog[]
+}         
+
+model Post {
+  id: Int @id
+  title: String
+  tags: String[]
+  blog: Blog
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190603131003)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190603131003'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
